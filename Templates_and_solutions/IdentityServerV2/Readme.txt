
This is the Authorization Server (AS) that can be publicly posted.

It works as-is, and does the following:
> Identity management
> Authentication
> Token issuing

For browser clients, and programmatic (web service) clients.

Authenticated browser clients receive a cookie.

Authenticated web service clients receive a token.
